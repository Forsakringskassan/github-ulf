name: NPM PR Preview

on: [workflow_call]

concurrency: preview-${{ github.ref }}

jobs:
  should-run:
    runs-on: ubuntu-latest
    outputs:
      run_job: ${{ steps.check.outputs.run_job }}
      has_build_docs: ${{ steps.check-build-docs.outputs.has_build_docs }}
    steps:
      - name: Check if workflow should run
        id: check
        run: |
          echo "event_name=${{ github.event_name }}"
          echo "event_action=${{ github.event.action }}"

          if [[ "${{ github.event_name }}" == "pull_request" && \
                ( "${{ github.event.action }}" == "opened" || \
                  "${{ github.event.action }}" == "reopened" || \
                  "${{ github.event.action }}" == "synchronize" || \
                  "${{ github.event.action }}" == "closed" ) ]]; then
            echo "run_job=true" >> $GITHUB_OUTPUT
          else
            echo "run_job=false" >> $GITHUB_OUTPUT
          fi
      - name: Checkout
        if: steps.check.outputs.run_job == 'true'
        uses: actions/checkout@v7
      - name: Check for build:docs script
        id: check-build-docs
        if: steps.check.outputs.run_job == 'true'
        run: |
          if jq -e '.scripts["build:docs"]' package.json > /dev/null 2>&1; then
            echo "has_build_docs=true" >> $GITHUB_OUTPUT
          else
            echo "has_build_docs=false" >> $GITHUB_OUTPUT
          fi

  deploy-preview:
    needs: should-run
    if: ${{ needs.should-run.outputs.run_job == 'true' && needs.should-run.outputs.has_build_docs == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24.x
          cache: "npm"
      - name: Install and build
        if: github.event.action != 'closed'
        run: |
          npm ci
          npm run build
      - name: Build documentation
        if: github.event.action != 'closed'
        run: npm run build:docs
      - uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./public/
          preview-branch: gh-pages
          umbrella-dir: pr-preview
          action: auto
